CREATE TABLE wormholes (
    id INTEGER PRIMARY KEY,
    from_map TEXT NOT NULL,
    to_map TEXT NOT NULL,
    x REAL,
    y REAL,
    theta REAL
);

-- Add example data
INSERT INTO wormholes (from_map, to_map, x, y, theta) VALUES ('room1', 'room2', 1.0, 2.0, 0.0);
INSERT INTO wormholes (from_map, to_map, x, y, theta) VALUES ('room2', 'room3', -1.0, 0.5, 1.57);
-- Add more wormholes as needed.

.exit
